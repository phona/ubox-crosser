# Spec: Version History Section

## Acceptance Criteria

1. `README.md` contains a `## Version History` section.
2. The section appears after `## Architecture`.
3. The section contains at least one version entry: `v0.0.1 — sisyphus pipeline test`.
4. Each entry is a bulleted list item with the version in bold.
5. No existing README content is removed or altered.
